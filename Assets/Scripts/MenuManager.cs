using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuManager : MonoBehaviour
{
    // handles loading of menus
    // 
    // Start menu
    // New game (select slot)
    // Load game (select slot)
    // Option menu (from start menu)
    // Home scene (before enter map scene)
    // In-game Option menu

    public void LoadMenuModule(){
        
    }
}
